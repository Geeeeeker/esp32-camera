name: Build examples

on:
  push:
    branches:
      - master
  pull_request:

  workflow_dispatch:
    inputs:
      idf_ver:
        description: 'ESP-IDF Version'
        required: true
        type: choice
        default: 'latest'
        options:
          - release-v5.1
          - release-v5.2
          - release-v5.3
          - release-v5.4
          - release-v5.5
          - release-v6.0
          - latest
      idf_target:
        description: 'ESP32 Target Board'
        required: true
        type: choice
        default: 'esp32'
        options:
          - esp32
          - esp32s2
          - esp32s3
          - esp32c2
          - esp32c3
      camera_model:
        description: 'Camera Model'
        required: true
        type: choice
        default: 'OV2640'
        options:
          - OV2640
          - OV3660
          - OV5640
          - OV7670
          - OV7725
          - NT99141
          - GC032A
          - GC0308
          - GC2145
          - BF3005
          - BF20A6
          - SC101IOT
          - SC030IOT
          - SC031GS
          - HM0360
          - HM1055
      custom_pins:
        description: 'Custom GPIO pins (KEY=VAL,KEY2=VAL2)'
        required: false
        type: string
        default: ''

jobs:
  build-examples:
    name: Build for ${{ matrix.idf_target }} on ${{ matrix.idf_ver }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        idf_ver: ${{ fromJSON(github.event_name == 'workflow_dispatch' && format('["{0}"]', github.event.inputs.idf_ver) || '["release-v5.1", "release-v5.2", "release-v5.3", "release-v5.4", "release-v5.5", "release-v6.0", "latest"]') }}
        idf_target: ${{ fromJSON(github.event_name == 'workflow_dispatch' && format('["{0}"]', github.event.inputs.idf_target) || '["esp32", "esp32s2", "esp32s3", "esp32c2", "esp32c3"]') }}
        camera_model: ${{ fromJSON(github.event_name == 'workflow_dispatch' && format('["{0}"]', github.event.inputs.camera_model) || '["OV2640"]') }}
        exclude:
          - idf_ver: "release-v5.1"
            idf_target: "esp32c2"
          - idf_ver: "release-v5.2"
            idf_target: "esp32c2"

    container: espressif/idf:${{ matrix.idf_ver }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: 'true'

      - name: Build with ESP-IDF
        env:
          IDF_TARGET: ${{ matrix.idf_target }}
          CAMERA_MODEL: ${{ matrix.camera_model }}
          CUSTOM_PINS: ${{ github.event.inputs.custom_pins }}
        shell: bash
        working-directory: examples/camera_example
        run: |
          . ${IDF_PATH}/export.sh

          export PEDANTIC_FLAGS="-DIDF_CI_BUILD -Werror -Werror=deprecated-declarations -Werror=unused-variable -Werror=unused-but-set-variable -Werror=unused-function"
          export EXTRA_CFLAGS="${PEDANTIC_FLAGS} -Wstrict-prototypes"
          export EXTRA_CXXFLAGS="${PEDANTIC_FLAGS}"

          # 启用摄像头型号支持
          echo "CONFIG_${CAMERA_MODEL}_SUPPORT=y" >> sdkconfig.defaults

          # 写入自定义引脚配置
          if [[ -n "$CUSTOM_PINS" ]]; then
            echo "Applying custom GPIO pins: $CUSTOM_PINS"
            IFS=',' read -ra PAIRS <<< "$CUSTOM_PINS"
            for pair in "${PAIRS[@]}"; do
              key=$(echo "$pair" | cut -d'=' -f1)
              val=$(echo "$pair" | cut -d'=' -f2)
              echo "CONFIG_${key}=${val}" >> sdkconfig.defaults
            done
          fi

          idf.py build

      # 🔥 新增步骤：上传构建产物（固件文件）
      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ matrix.idf_target }}-${{ matrix.camera_model }}-${{ matrix.idf_ver }}
          path: |
            examples/camera_example/build/*.bin
            examples/camera_example/build/bootloader/*.bin
            examples/camera_example/build/partition_table/*.bin
          if-no-files-found: error
          retention-days: 7
